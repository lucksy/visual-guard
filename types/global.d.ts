// Ambient type declarations for Visual Guard.
// Intentionally minimal; populated as the engine grows. The `export {}` keeps this
// a module (no global scope pollution) and gives `tsc` an input on the empty tree.
export {};
